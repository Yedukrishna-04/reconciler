services:
  - type: web
    name: onelab-payments-reconciler
    env: python
    plan: free
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python start_render.py
    healthCheckPath: /health
